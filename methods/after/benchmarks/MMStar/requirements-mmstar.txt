# Minimal deps for benchmarks/MMStar (data prep + scoring).
# Install: pip install -r requirements-mmstar.txt

datasets>=2.14.0
Pillow>=9.0.0
pandas>=1.5.0
jsonlines>=3.0.0
# Optional: only needed for --scoring vlmeval in mmstar_eval.py
openai>=1.0.0
